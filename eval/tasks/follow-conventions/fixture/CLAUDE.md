# コーディング規約

このリポジトリのコードを書くときは、必ず以下に従うこと。

1. **`// why:` コメント**: エクスポートする関数の直前には、その関数がなぜ存在するのかを
   説明する `// why:` で始まるコメントを付ける。
2. **エラーは AppError**: 例外を投げるときは必ず `src/errors.ts` の `AppError` を
   使うこと。素の `Error` を `throw new Error(...)` の形で投げるのは禁止。
   エラーコードは大文字スネークケース(例: `INVALID_INPUT`)とする。
